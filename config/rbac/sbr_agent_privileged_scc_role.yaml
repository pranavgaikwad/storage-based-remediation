apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: sbr-agent-privileged-scc
rules:
- apiGroups:
  - security.openshift.io
  resources:
  - securitycontextconstraints
  resourceNames:
  - privileged
  verbs:
  - use
